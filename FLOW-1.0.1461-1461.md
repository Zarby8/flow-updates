# FLOW 1.0.1461
- PRODUCE BREAKDOWN → FILE TO NAS now works: completed client breakdowns file into the correct NAS game folder (Chris-Clients_<season>/<Client>/<Season>/<Game>_<date>), which the old build could never find. Set NAS FILING to AUTOMATIC or ASK EVERY TIME in the export panel.
- NAS filing now shows a FILING TO NAS… status while it copies and a "Filed to NAS" confirmation when done, instead of failing silently.
