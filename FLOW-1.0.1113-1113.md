# FLOW 1.0.1113

- IQ Library clips no longer start with a black screen — the preview player's seek tolerance now matches the other playback paths.
- Breakdown Export overlay text is smoother: all text sizes unified, raster supersampling raised to 3x, and the panel background is more solid during export.
- NHL clip notes no longer carry SportsCode period-time stamps (e.g. "P2-18:42") that leaked from the database into the overlay and export.
- Ghost shift import copies the .flowaipkg to ~/Desktop/FLOW/ with a canonical filename after completion.
