# FLOW 1.0.1276

FLOW project packages now use the FLOW logo in Finder. The signed release validates the package icon declarations and included icon resource.

Includes the provider-media import and export improvements from earlier releases.
